NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password_here
OPENAI_API_KEY=sk-...
KNOWLEDGE_BASE_DIR=skills/knowledge

# Optional — only needed to run `python -m pipeline.enrich_skills` (regenerate
# staging/skills/ from raw SKILL.md source files). Not required to run the
# MCP server or load the already-enriched staging/skills/ that ships in this repo.
# SKILLS_LIB_PATH=/path/to/your/skill-library/skills
